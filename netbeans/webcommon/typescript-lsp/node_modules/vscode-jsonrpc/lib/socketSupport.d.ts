import { MessageReader } from './messageReader';
import { MessageWriter } from './messageWriter';
export interface SocketTransport {
    onConnected(): Promise<[MessageReader, MessageWriter]>;
}
export declare function createClientSocketTransport(port: number, encoding?: string): Promise<SocketTransport>;
export declare function createServerSocketTransport(port: number, encoding?: string): [MessageReader, MessageWriter];
